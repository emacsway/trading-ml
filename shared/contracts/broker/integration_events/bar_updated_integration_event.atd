(* Wire contract: broker/lib/application/integration_events/bar_updated_integration_event.ml *)

type instrument_view_model = {
  ticker : string;
  venue : string;
  ?isin : string option;
  ?board : string option;
}

type candle_view_model = {
  ts : string;
  open_ <json name="open"> : string;
  high : string;
  low : string;
  close : string;
  volume : string;
}

type t = {
  instrument : instrument_view_model;
  timeframe : string;
  candle : candle_view_model;
}
