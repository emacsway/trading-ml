(* Wire contract: portfolio_management/lib/application/commands/apply_bar_command.ml *)

type candle_dto = {
  ts : string;
  open_ <json name="open"> : string;
  high : string;
  low : string;
  close : string;
  volume : string;
}

type t = {
  instrument : string;
  timeframe : string;
  candle : candle_dto;
}
