(* Wire contract: paper_broker/lib/application/integration_events/order_rejected_integration_event.ml *)

type t = {
  correlation_id : string;

  placement_id <doc text="Opaque correlation token from the originating submit_order_command. Account releases the reservation on this event."> : int;

  reason : string;
}
