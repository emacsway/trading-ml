(** Why3 companion for [reservation_id.mli] — the wrapper enforces
    [reservation_id > 0]. *)

module Reservation_id
  use int.Int

  type t

  function to_int (r: t) : int

  axiom positive : forall r: t. to_int r > 0
end
